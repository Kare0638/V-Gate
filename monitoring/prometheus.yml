global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "vgate"
    metrics_path: "/metrics"
    honor_labels: true
    static_configs:
      - targets: ["vgate:8000"]
        labels:
          service: "vgate"
