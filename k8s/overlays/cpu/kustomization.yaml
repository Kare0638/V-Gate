# CPU overlay: dry-run workers, no GPU required. This is what runs on kind.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: gateway-patch.yaml
  - path: worker-patch.yaml
